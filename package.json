{
  "name": "autoagent-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "preview": "cd frontend && npm run preview -- --host 0.0.0.0 --port ${PORT:-10000}",
    "start": "cd backend && npm start"
  }
}
